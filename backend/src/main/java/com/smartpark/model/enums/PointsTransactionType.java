// Defines the type of points transaction recorded in the points ledger.
package com.smartpark.model.enums;

public enum PointsTransactionType {
    EARNED,
    SPENT,
    DIVIDED,
    POOLED,
    EXPIRED
}
